import buildApplicationContext from "./buildApplicationContext";
import loaders from "./loaders";
import annotations from "./annotations";

export { buildApplicationContext, loaders, annotations };

export default { buildApplicationContext, loaders, annotations };
